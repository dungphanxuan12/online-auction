package com.auction.validation;

public interface DateValidator {
	/**
	 * common method to check {String} data is valid or not
	 * 
	 * @param date
	 * @return boolean
	 */
	boolean isValid(String date);
}
